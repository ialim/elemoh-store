import CreateProduct from "../components/create-product";

const CreateProducts = () => {
  return <CreateProduct />;
};

CreateProducts.displayName = "CreateProducts";

export default CreateProducts;
